import { TriggerNav } from './trigger-nav';

describe('TriggerNav', () => {
  it('should create an instance', () => {
    const directive = new TriggerNav();
    expect(directive).toBeTruthy();
  });
});
